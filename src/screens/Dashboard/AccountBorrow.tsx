import styled from "@emotion/styled";
import React from "react";

interface IProps {}

const Root = styled.div`
  display: flex;
  flex-direction: column;
`;

const AccountBorrow: React.FC<IProps> = () => {
  return <Root>AccountBorrow</Root>;
};
export default AccountBorrow;
